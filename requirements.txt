
opencv-python-headless==4.8.1.78
numpy==1.24.3
ultralytics==8.0.196

torch==2.0.1

# HTTP requests and image processing for live feeds
requests==2.31.0
Pillow==10.0.0

# Web framework
Flask==2.3.3

# WSGI Server for Azure App Service (required for GitHub deployment)
gunicorn==21.2.0

# Azure integration libraries
azure-identity==1.15.0
azure-storage-blob==12.19.0

# Environment and configuration management
python-dotenv==1.0.0

# CORS support for web APIs
flask-cors==4.0.0

# Production logging
structlog==23.1.0

# Health check endpoint for Azure monitoring
flask-healthz==1.0.1

# Additional production dependencies (usually installed with Flask, but specified for clarity)
Werkzeug==2.3.7
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7
blinker==1.6.2

# Development and testing (optional for production)
pytest==7.4.2